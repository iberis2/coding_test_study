## 🧩 문제 설명

- [베스트앨범](https://school.programmers.co.kr/learn/courses/30/lessons/42579)

### 💡 풀이 과정 (Algorithm)

1. **장르별로 총 재생 횟수를 알아야 우선순위를 정하겠네? 장르별 합계를 담을 Map이 필요할듯**
   - `genreHash`를 만들어 `{장르: 총 재생수}`를 저장한다.
2. **장르 안에서도 노래들을 재생수 순으로 줄 세워야 하니까, 장르별 노래 정보를 모을 Map도 따로 파야할듯, 이때 value값으론 객체를 집어넣자**
   - `playHash`에 `{장르: [{id, count}, ...]}` 형태로 데이터를 쌓는다.
3. **이제 남은건 정렬, 먼저 장르 합계가 큰 순서대로 장르 리스트를 정렬하자.**
   - `genreHash`를 배열로 변환해 내림차순 정렬한다.
4. **정렬된 장르를 하나씩 돌면서, 그 안의 노래들을 재생수 순으로 또 정렬하고 딱 2개만 끊어서 가져오면 끝**
   - 재생 수가 같다면 고유 번호(`id`)가 낮은 순서대로 가져오는 예외 조건도 챙겨야 함

### 💻 코드 (Code)

```javascript
/*
    genres, plays
    
    classic -> 500 + 150 + 800 = 1450
    pop -> 600 + 2500 = 3100
*/

function solution(genres, plays) {
  // TODO: 장르 합 대소 구분, 장르별 횟수 구분
  let genreHash = new Map();
  let playHash = new Map();

  for (let i = 0; i < genres.length; i += 1) {
    genreHash.set(genres[i], (genreHash.get(genres[i]) || 0) + plays[i]);

    if (!playHash.has(genres[i])) playHash.set(genres[i], []);
    playHash.get(genres[i]).push({ id: i, count: plays[i] });
  }

  const sortedGenreHash = [...genreHash.entries()].sort((a, b) => b[1] - a[1]);

  const result = [];

  sortedGenreHash.forEach(([genre, total]) => {
    const topTwosongs = playHash
      .get(genre)
      .sort((a, b) => b.count - a.count)
      .slice(0, 2);

    topTwosongs.forEach((s) => result.push(s.id));
  });

  return result;
}
```

### 🏁 회고 (Retrospective)

- **효율성**: 문제 풀이 과정에서 해싱하여 정리해 놓은 값들을 정렬했기에
  **N\*log(N)** 으로 도출

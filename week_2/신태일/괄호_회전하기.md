## 🧩 문제 설명

- [괄호 회전하기](https://school.programmers.co.kr/learn/courses/30/lessons/76502)

### 💡 풀이 과정 (Algorithm)

1. **문자열을 왼쪽으로 한 칸씩 민다?**

- `s.slice(1) + s[0]` 조합으로 헬퍼함수로 빼놓기

2. **회전할 때마다 이게 올바른 괄호인지 검사한다? 이건 너무 스택**

- 스택 전용 헬퍼 함수 만들 생각
- 소, 중, 대괄호를 record로 묶어서 활용하자

3. **판별하고 갯수세기**

- 스택 헬퍼함수 결과값이 빈 배열이면 cnt += 1

### 💻 코드 (Code)

```javascript
/*
인덱스 1번까지짜르고 기존 인덱스 0번 끝에 붙히기
순차적으로 검사해서 양 옆 짝꿍 찾으면 탈출시키기
자기 짝 찾으면 slice
*/

function moveLeft(target) {
  return target.slice(1) + target[0];
}

function compareTarget(target) {
  let stack = [];
  let record = {
    "(": ")",
    "{": "}",
    "[": "]",
  };

  for (let i = 0; i < target.length; i += 1) {
    const top = stack[stack.length - 1];

    if (stack.length > 0 && record[top] === target[i]) {
      stack.pop();
    } else {
      stack.push(target[i]);
    }
  }
  return stack;
}

function solution(s) {
  let result = 0;

  for (let i = 0; i < s.length; i += 1) {
    if (compareTarget(s).length == 0) {
      result += 1;
    }

    s = moveLeft(s);
  }
  return result;
}
```

### 🏁 회고 (Retrospective)

- **효율성**: O(N^)이 나오는데 이게 최선일듯 싶다.. 결과값 낼 때도 어떻게든 반복문을 돌려야 하니

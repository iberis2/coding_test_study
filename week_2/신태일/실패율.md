## 🧩 문제 설명

- [실패율](https://school.programmers.co.kr/learn/courses/30/lessons/42889)

### 💡 풀이 과정 (Algorithm)

1. **문제에서 시키는 대로 우선 1라운드부터 스테이지별로 탈락자 확률을 구해보자.**

- 전체 인원수(`stages.length`)를 분모로 잡고, 현재 스테이지에 멈춰있는 사람을 분자로 써서 실패율을 구하기

2. **그다음 라운드 탈락자를 구할 땐 이전 라운드 탈락자를 제외해야겠네?**

- 이번 스테이지에서 탈락한 사람 수만큼 전체 인원에서 빼주면, 다음 라운드 계산할 때 '도달한 사람' 수만 남게 되니 이 방식 반복문 돌려야지

3. **도달한 사람이 0명인 스테이지는 0으로 처리하는 예외조건 걸기**

- `playerSum == 0`인 상황에서는 삼항 연산자로다가

4. **마지막으로 구한 확률들을 실패율 높은 순으로 줄 세우기만 하면 끝**

- 스테이지 번호랑 실패율을 같이 묶어서 저장하고 value별로 정렬

### 💻 코드 (Code)

```javascript
/*
사람수 -> stages.length
*/

function solution(N, stages) {
  let result = []; // 실패율
  let playerSum = stages.length;

  for (let i = 1; i <= N; i += 1) {
    let curStageNum = stages.filter((s) => s == i).length;

    let failRate = playerSum == 0 ? 0 : curStageNum / playerSum;

    result.push({ key: i, value: failRate });

    playerSum -= curStageNum;
  }

  return result.sort((a, b) => b.value - a.value).map((v) => v.key);
}
```

### 🏁 회고 (Retrospective)

- **효율성**: 500 \* 200,000 이라 그리 좋아보이진 않는다.. 역시 좀 소거해서 했어야했나 -> 나중에 이 점을 유념하고 다시 풀어보자
